# Analytics boundary

Future Python code for point-in-time feature generation, probabilistic forecasting, simulation, backtesting and optimisation. It returns versioned candidate artefacts; the backend validates and records them.
